# Metrics

`npm run metrics`はD1の匿名イベントを集計し、JSONで出力します。

| Field              | Meaning                                   |
| ------------------ | ----------------------------------------- |
| `users`            | 訪問した匿名セッション数                  |
| `outbound_users`   | 1つ以上のサービスを開いた匿名セッション数 |
| `outbound_choices` | セッションとサービスの異なる組み合わせ数  |
| `returned`         | 別日に再訪した匿名セッション数            |
| `tools.*`          | 各サービスを開いた匿名セッション数        |

主要比率は`outbound_users / users`です。ページ表示だけを価値完了とせず、外部サービスを開いた操作を送客として扱います。

検索語や絞り込み条件は送信しません。イベントは日単位で重複排除し、35日後に削除します。分母0の比率は0です。
